model:
  name: "mlx-community/Qwen1.5-0.5B-Chat"
  max_tokens: 512

dataset:
  path: "data/train.jsonl"
  format: "instruct"
  train_split: 0.8

training:
  epochs: 10
  batch_size: 4
  learning_rate: 1e-5
  lora_rank: 4
  lora_alpha: 8
  lora_dropout: 0.0
  save_every: 10
  output_dir: "runs/"

eval:
  metrics: ["loss", "rouge"]

logging:
  level: "INFO"
  wandb: false
